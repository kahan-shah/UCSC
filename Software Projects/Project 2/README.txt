This projects showcases a variety of string functions. Such as a function censors the word the user chooses, a function fixes the title of books movies and articles, a function that finds the longest word in a sentence.
